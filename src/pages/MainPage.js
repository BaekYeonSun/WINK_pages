import React from 'react';

const MainPage = () => {
    return (
        <div>
            <h2 style={styles.title}>메인 페이지</h2>
        </div>
    );
};

const styles = {
    title: {
        textAlign: 'center'
    },
};

export default MainPage;